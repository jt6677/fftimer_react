// import { SIGN_UP } from "../actions/types";

// const initialState = {};

// export default (state = initialState, { type, payload }) => {
//   switch (type) {
//     case SIGN_UP:
//       return { ...state, ...payload };

//     default:
//       return state;
//   }
// };
